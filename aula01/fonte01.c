#include <stdio.h>
#define B 4
int main() {
    int a = 10;
    int resultado = a + B;

    printf("O resultado da soma é: %d\n", resultado);

    return 0; // Indica que o programa terminou com sucesso
}